/** Shared changelog index + detail layout (keep in sync across routes). */
export const changelogMainColumnClassName =
  "mx-auto w-full max-w-3xl px-4 py-12 sm:px-6 sm:py-16"

export const changelogBreadcrumbRowClassName =
  "mb-6 flex min-h-8 w-full flex-wrap items-center"

/** Space below the page intro block (title + description / meta). */
export const changelogPageHeaderSectionClassName = "mb-10 sm:mb-12"
